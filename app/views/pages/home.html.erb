<div id="home-parallax">
  <div class="bgimg-1">
    <div class="caption">
      <div class="home-header">
        <h1>SOL'UT</h1>
        <div class="container-btn-top-page" style="position:absolute;bottom:30px;right:160px;">
            <% if user_signed_in? %>
            <% else  %>
            <span class="border"><%= link_to "LOGIN", new_user_session_path, class:"btn-login" %></span>
            <% end %>
            <span class="border"><%= link_to "DISCOVER", events_path, class:"btn-login" %></span>
        </div>
      </div>
    </div>
  </div>
</div